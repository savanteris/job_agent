name: Run Daily Job Agent

on:
  schedule:
    # Uruchamia się codziennie o 09:00 UTC
    - cron: '0 9 * * *'
  workflow_dispatch: # Pozwala uruchomić skrypt ręcznie w każdej chwili

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run script
        env:
          SMTP_USER: ${{ secrets.SMTP_USER }}
          SMTP_PASSWORD: ${{ secrets.SMTP_PASSWORD }}
        run: python job_agent_email.py

      - name: Save seen offers state
        uses: stefanzweifel/git-auto-commit-action@v4
        with:
          commit_message: "Auto-update seen offers"
